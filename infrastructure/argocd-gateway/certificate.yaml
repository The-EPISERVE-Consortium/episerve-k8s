apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-tls
  namespace: argocd
spec:
  secretName: argo-tls
  dnsNames:
    - argok8.medicalbioinformatics.de
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
