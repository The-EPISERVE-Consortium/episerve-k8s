apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls
spec:
  secretName: GATEWAY_SECRET_NAME
  dnsNames:
    - GATEWAY_HOSTNAME
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    